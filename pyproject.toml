[tool.pytest.ini_options]
pythonpath = ["."]
# Only tests/ is the test suite; eval/ holds scripts, not tests. Scoped explicitly so a
# bare `pytest` from the repo root never tries to collect anything under eval/.
testpaths = ["tests"]
